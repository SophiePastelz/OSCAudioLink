* Due to framework update most tab icons and text now look bigger
* Some backend optimization in the `preview` tab
* Changed the `OSC Config` tab's icon to be a cogwheel
* Added a new tab and functionality called `AudioLinkToOSC`
* * In this tab you can listen to AudioLink data in a world with AudioLink in them and send out data from one of 4 audiolink's bands to a OSC address
* * You will need to add a specially made prefab in the vrcfury variant for this to function properly
* * * This will obviously only properly work in world with audiolink and on avatars with the AudiolinkToOSC Prefab

### `Config` 
* Added new parameters for the new AudioLinkToOSC features